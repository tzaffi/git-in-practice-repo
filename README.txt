Top Level Directory Structure

book -- the virtualenv directory for running the book code
     	To enter into virtualenv -from top level- run the command:
source ./book/bin/activate
        To exit the virtualevn run:
deactivate

chp2 -- the project we created in chapter 2 
